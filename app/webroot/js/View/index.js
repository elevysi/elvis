(function($) {
  $('#autocomplete').autocomplete({
        source: "users/index.json"
  });
})(jQuery);


(function($) {
	  $('#autocompleteinvestigators').autocomplete({
	        source: "projects/index.json"
	  });
})(jQuery);
